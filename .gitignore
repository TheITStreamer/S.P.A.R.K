# Build output (huge, regenerated by every build)
src-tauri/target/

# Dependencies (regenerated by `npm install`)
node_modules/

# Tauri generated schemas
src-tauri/gen/

# Logs & OS junk
*.log
npm-debug.log*
Thumbs.db
.DS_Store

# Internal planning / design notes (not for public repo)
docs/

# Patch notes live in GitHub releases, not the repo
PATCH_NOTES.md
CHANGELOG-post*.md

# Working notes for the build currently on the bench (+ archived sessions)
Current_Build.txt
Current_Build_ARCHIVE*.txt
TEST_CHECKLIST.txt
h --force -u origin main